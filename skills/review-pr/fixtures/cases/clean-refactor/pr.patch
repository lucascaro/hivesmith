diff --git a/src/geometry.py b/src/geometry.py
index 4444444..5555555 100644
--- a/src/geometry.py
+++ b/src/geometry.py
@@ -2,19 +2,19 @@
 import math


-def area_circle(r: float) -> float:
-    if r < 0:
+def circle_area(radius: float) -> float:
+    if radius < 0:
         raise ValueError("radius must be non-negative")
-    return math.pi * r * r
+    return math.pi * radius * radius


-def area_rect(w: float, h: float) -> float:
-    if w < 0 or h < 0:
+def rectangle_area(width: float, height: float) -> float:
+    if width < 0 or height < 0:
         raise ValueError("dimensions must be non-negative")
-    return w * h
+    return width * height


-def area_tri(b: float, h: float) -> float:
-    if b < 0 or h < 0:
+def triangle_area(base: float, height: float) -> float:
+    if base < 0 or height < 0:
         raise ValueError("dimensions must be non-negative")
-    return 0.5 * b * h
+    return 0.5 * base * height
diff --git a/tests/test_geometry.py b/tests/test_geometry.py
index 6666666..7777777 100644
--- a/tests/test_geometry.py
+++ b/tests/test_geometry.py
@@ -1,30 +1,30 @@
 import math
 import pytest
-from src.geometry import area_circle, area_rect, area_tri
+from src.geometry import circle_area, rectangle_area, triangle_area


 def test_circle_area():
-    assert area_circle(2) == pytest.approx(math.pi * 4)
+    assert circle_area(2) == pytest.approx(math.pi * 4)


 def test_circle_area_zero():
-    assert area_circle(0) == 0
+    assert circle_area(0) == 0


 def test_circle_area_negative():
     with pytest.raises(ValueError):
-        area_circle(-1)
+        circle_area(-1)


 def test_rectangle_area():
-    assert area_rect(3, 4) == 12
+    assert rectangle_area(3, 4) == 12


 def test_rectangle_area_negative():
     with pytest.raises(ValueError):
-        area_rect(-1, 2)
+        rectangle_area(-1, 2)


 def test_triangle_area():
-    assert area_tri(4, 5) == 10
+    assert triangle_area(4, 5) == 10


 def test_triangle_area_negative():
     with pytest.raises(ValueError):
-        area_tri(-1, 2)
+        triangle_area(-1, 2)
