---
type: added
bump: minor
---
- **A plan lane that starts from an ambiguous prompt and ends at another agent.** Three skills compose into a path from "a vague sentence in chat" to "a plan a fresh agent in another worktree executes without re-asking anything."
  - **`/feature-plan` is now dual-mode.** `/feature-plan 42` behaves exactly as before — spec-driven, stage-gated, writes the exec plan. `/feature-plan "make the auth backend pluggable"` (or a bare invocation with no `PLAN`-stage spec) enters standalone mode: no issue, no spec, no stage guard, works in any repo or none. Both modes now run an **interrogation loop** — grounded in the code first, batched questions (max 3 rounds, ≤4 per round), with an explicit stop rule: stop asking when no remaining unknown would change the file list, the test list, or a public interface. Every answer lands in `## Decisions` with the rejected alternative, which is what makes the plan survive the session boundary. Draft format is now size-adaptive: inline text for short plans, HTML via `/plan-html` for anything medium or with diagram-worthy content; `--text` / `--html` force either way.
  - **`/feature-plan-review`** verifies a plan against the code it claims to touch — every path must exist, every referenced helper must exist with the assumed shape, every changed function's callers get grepped for missed call sites — then strips speculative scope (single-implementation interfaces, config for constants, scaffolding "for later") and resolves the remaining open questions. It edits the plan in place, appends a `## Review log`, and never touches production code. Linear by default; fans out to three `hs-reviewer` subagents (grounding / gaps / YAGNI) only on plans over ~200 lines or ~10 files.
  - **`/feature-plan-handoff`** is a hard readiness gate — refuses on unresolved open questions, vague or missing tests, missing verification commands, an unreviewed plan, or missing non-goals, reporting every failure at once — then prints a copy-pasteable pickup block naming the plan path, the repo, and the exact prose a fresh agent on any harness can be handed.
- **Standalone plans live at `~/.hivesmith/plans/<slug>.md`**, durable across worktrees and projects, with a schema at `skills/feature-plan/plan-template.md` (frontmatter `slug`/`title`/`status`/`created`/`source`/`repo`, plus `## Decisions`, `## Non-goals`, `## Verification`, `## Review log`, `## Progress`). A plan has exactly one home: the exec plan when a product spec exists, `~/.hivesmith/plans/` otherwise — never mirrored between the two.
- **`docs/exec-plans/_template.md` gains a `## Verification` section** (and the `templates/` copy with it) — exact runnable commands that prove the change works, which `/feature-plan` now fills and `/feature-plan-handoff` gates on. Exec plans scaffolded from the older template are backfilled by `/feature-plan-review`. Non-goals stay in the product spec, where `/feature-qa` already reads them.
- **The `/plan-html` call sequence is now documented once**, in `skills/plan-html/SKILL.md`, and referenced by `/feature-loop`, `/feature-plan`, and `/feature-plan-review` instead of restated in each. The canonical version keeps the `template.html` existence guard and the HTML → native-plan-mode → inline-text fallback chain that the duplicated copies had drifted away from.
