---
type: added
bump: minor
---
- **Hive brain — cross-project second brain at `~/.hivesmith/brain/`.** A git-tracked, file-based, scope-tagged lesson store that hivesmith skills read at the start of a run and append to at convergence. Lessons are tagged `universal | ecosystem | user | project`; retrieval filters by active-project context (no cross-repo bleed). Shipped: `scripts/brain/{lib.sh,read.sh,append.sh,redact.sh,index.sh,yaml.py}` helpers, `templates/brain/{SCHEMA,README,INDEX,.gitignore}` templates, two new skills `/brain-promote` (the only path that broadens scope) and `/brain-garden` (regen index, archive expired entries, surface promotion + dedupe candidates), and integration into six existing skills (read in `feature-research` / `feature-plan` / `review-pr`; append on convergence in `feature-implement` / `review-pr` / `review-loop`). Brain content is **untrusted at load** — wrapped in `<project-memory untrusted="true">` delimiters. Write-time redaction (AWS keys, GitHub tokens, private keys, ≤25-line code-fence cap, optional `gitleaks` shell-out). Tiered `INDEX.md` (HOT ≤50 lines + ALL ≤2000) for context-bloat control. `install.sh` symlinks helpers into `~/.hivesmith/bin/`.
